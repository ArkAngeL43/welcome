import os
import sys 
import time 
import requests
import colorama
from colorama import Fore 
from colorama import init 
from datetime import datetime


init()

#843363793962532865
#844075047978926090
#866173576040939541


B = str(input(" ==> "))

if '427810' in B:
             os.system(' clear ')
             startTime = datetime.now()
             os.system(' cd markAI && mpg123 disspam.mp3 ')
             os.system(' clear ')
             def restart_program():
                python = sys.executable
                os.execl(python, python, * sys.argv)
                curdir = os.getcwd()
             import time
             from tqdm import tqdm
             def loading():
                for _ in tqdm(range(100), desc="Loading...", ascii=False, ncols=75):
                   time.sleep(0.01)
                print("Loading Done!")


             if __name__ == "__main__":
                 loading()

             payload = {
                'content': " Helloooo and a good afternoon to eveyone "
             }

             header = {
                'authorization': 'ODQyNjA5ODI0NzY0MjY0NDUw.YQdiOw.7pGubTlwRlI9eL2Svc9kJZ61FT4'
             }
             time.sleep(1)
             os.system(' clear ')
             print("="*40)
             os.system('figlet DISCORD ')
             os.system('figlet     LAZYYY ')
             print("="*40)
             print(" [+] Notice: when animatio loads is when message sends [+] ")
             print(" [-] animation is to hold it from sending all at once  [-] ")
             print(" [-] to prevent bans, spams, and to make sure every    [-] ")
             print(" [-] sends corectly and is also formatted correctly    [-] ")
             time.sleep(1)
             print(" [!] sending messages [!] ")
             #print(" [+] message is" + payload)
             #time.sleep(1) for close ending to allow discord to communicate properly
             # tested with over 18+ people and didnt send full messages 
             # cool down takes affect 
             # time.sleep(1) will help the cooldown apply later rather than eralier 
             # 
             #aces link 
             #https://discord.com/api/v9/channels/858213051856584735/messages # specified 4735 end digits
             #r = requests.post("https://discord.com/api/v9/channels/862377272853004309/messages", data=payload, headers=header)
             time.sleep(1)
             def loading():
                for _ in tqdm(range(100), desc="Sending Msg...", ascii=False, ncols=75):
                   time.sleep(0.01)
             IDLIST = ["843363793962532865","844075047978926090","866173576040939541"]
             if __name__ == "__main__":
               for x in IDLIST:
                  loading()
                  r = requests.post(f"https://discord.com/api/v9/channels/{x}/messages",data=payload, headers=header)
                  time.sleep(1.5)
                  def loading():
                     for _ in tqdm(range(100), desc="Sending Msg...", ascii=False, ncols=75):
                        time.sleep(0.01)
             if __name__ == "__main__":
                 loading()  